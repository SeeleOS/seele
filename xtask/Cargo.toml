[package]
name = "xtask"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
bootloader = { version = "0.11", default-features = false, features = ["uefi"] }
libc = "0.2"
ovmf-prebuilt = "0.2"
serde_json = "1.0"
