use crate::memory::page_table_wrapper::PageTableWrapped;

impl PageTableWrapped {
    /// Clone all the memory thats in [`self`] to [`target`]
    pub fn clone_memory(&mut self, target: PageTableWrapped) {
        unimplemented!()
    }
}
