# Name Service Switch configuration file.
# Keep account lookups on plain files until the guest fully supports
# the extra systemd NSS backends they would otherwise pull in.

passwd: files
group: files
shadow: files
gshadow: files

publickey: files

hosts: files dns
networks: files

protocols: files
services: files
ethers: files
rpc: files

netgroup: files
